# MSFoundryAgentFormat — Python dependencies
# Install: pip install -r requirements.txt

# Azure AI Foundry — agent client & response format support
azure-ai-projects>=1.0.0b11
azure-identity>=1.17.0

# Pydantic — schema derivation for JSON schema response format
pydantic>=2.0.0

# Environment management
python-dotenv>=1.0.0
